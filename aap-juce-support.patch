diff --git a/CMakeLists.txt b/CMakeLists.txt
index 52f0a96..51ea011 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -40,8 +40,8 @@ set (CMAKE_OBJCXX_STANDARD 20)
 set (CMAKE_OBJCXX_STANDARD_REQUIRED ON)
 set (CMAKE_CXX_VISIBILITY_PRESET hidden)
 set (CMAKE_VISIBILITY_INLINES_HIDDEN ON)
-set (CMAKE_MINSIZEREL_POSTFIX -rm)
-set (CMAKE_RELWITHDEBINFO_POSTFIX -rd)
+#set (CMAKE_MINSIZEREL_POSTFIX -rm)
+#set (CMAKE_RELWITHDEBINFO_POSTFIX -rd)
 set (CMAKE_OPTIMIZE_DEPENDENCIES OFF)
 
 set (BUILD_SHARED_LIBS OFF)
@@ -183,6 +183,9 @@ target_include_directories (${PLUGIN_NAME} PRIVATE modules/fmt/include)
 target_include_directories (${PLUGIN_NAME} PRIVATE modules/ASIO/common)
 target_include_directories (${PLUGIN_NAME} PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/Source/Definitions)
 
+# this has to be placed before juce_generate_juce_header()...
+juce_add_modules(${AAP_JUCE_DIR}/aap-modules/aap_audio_processors)
+
 juce_generate_juce_header (${PLUGIN_NAME})
 
 target_compile_definitions (${PLUGIN_NAME} PRIVATE
@@ -191,7 +194,7 @@ target_compile_definitions (${PLUGIN_NAME} PRIVATE
 								JUCE_MODAL_LOOPS_PERMITTED=0
 								JUCE_WEB_BROWSER=0
 								JUCE_USE_FLAC=1
-								JUCE_USE_CURL=1
+								#JUCE_USE_CURL=1
 								JUCE_USE_MP3AUDIOFORMAT=0
 								JUCE_USE_LAME_AUDIO_FORMAT=0
 								JUCE_USE_WINDOWS_MEDIA_FORMAT=0
@@ -237,7 +240,7 @@ if (WIN32)
 	endforeach()
 endif()
 
-if(UNIX AND NOT APPLE)
+if(UNIX AND NOT APPLE AND NOT ANDROID)
 	target_link_libraries (${PLUGIN_NAME} PRIVATE curl)
 endif()
 
@@ -249,3 +252,49 @@ else()
 endif()
 
 install (TARGETS ${PLUGIN_NAME} DESTINATION "${dest}")
+
+# begin AAP specifics -->
+
+# They are needed in desktop too, for generate-aap-metadata.
+target_include_directories(${PLUGIN_NAME} PUBLIC
+		"${AAP_DIR}/include"
+		"${AAP_JUCE_DIR}/aap-modules"
+)
+
+target_compile_definitions(${PLUGIN_NAME} PUBLIC
+		JUCEAAP_HAVE_AUDIO_PLAYHEAD_NEW_POSITION_INFO=1
+		JUCE_PUSH_NOTIFICATIONS=1
+		JUCE_PUSH_NOTIFICATIONS_ACTIVITY="com/rmsl/juce/JuceActivity"
+)
+
+message("AAP_DIR: ${AAP_DIR}")
+message("AAP_JUCE_DIR: ${AAP_JUCE_DIR}")
+
+if (ANDROID)
+	find_library(log "log")
+	find_library(android "android")
+	find_library(glesv2 "GLESv2")
+	find_library(egl "EGL")
+	#set(cpufeatures_lib "cpufeatures")
+	#set(oboe_lib "oboe")
+
+	target_include_directories(${PLUGIN_NAME} PRIVATE
+			"${ANDROID_NDK}/sources/android/cpufeatures"
+			#"${OBOE_DIR}/include"
+	)
+
+	add_compile_definitions(
+			[[JUCE_ANDROID=1]]
+			[[JUCE_DONT_AUTO_OPEN_MIDI_DEVICES_ON_MOBILE=1]]
+	)
+
+	find_package(androidaudioplugin REQUIRED CONFIG)
+	target_link_libraries(${PLUGIN_NAME} PRIVATE
+			androidaudioplugin::androidaudioplugin
+			aap_audio_processors
+	)
+endif (ANDROID)
+
+target_link_libraries(${PLUGIN_NAME} PRIVATE aap_audio_processors)
+
+# <-- end AAP specifics
Submodule modules/gin contains modified content
diff --git a/modules/gin/modules/gin_gui/utilities/gin_layoutsupport.cpp b/modules/gin/modules/gin_gui/utilities/gin_layoutsupport.cpp
index 994d668685..4ede7f92ed 100644
--- a/modules/gin/modules/gin_gui/utilities/gin_layoutsupport.cpp
+++ b/modules/gin/modules/gin_gui/utilities/gin_layoutsupport.cpp
@@ -325,22 +325,22 @@ LayoutSupport::LayoutSupport (juce::Component& parent_, std::function<std::pair<
 
     if (refCount == 0)
     {
-        watcher = std::make_unique<FileSystemWatcher>();
-        watcher->coalesceEvents (100);
+        //watcher = std::make_unique<FileSystemWatcher>();
+        //watcher->coalesceEvents (100);
     }
 
     refCount++;
 
-    watcher->addListener (this);
+    //watcher->addListener (this);
 }
 
 LayoutSupport::~LayoutSupport()
 {
-    watcher->removeListener (this);
+    //watcher->removeListener (this);
 
     refCount--;
     if (refCount == 0)
-        watcher = nullptr;
+        ;//watcher = nullptr;
 }
 
 bool LayoutSupport::isLayoutSet()
@@ -420,7 +420,7 @@ void LayoutSupport::setLayout (const juce::Array<juce::File>& files)
         layoutFiles.add (f);
         contents.add ({ f.getFileName(), f.loadFileAsString() });
 
-        watcher->addFolder (f.getParentDirectory());
+        //watcher->addFolder (f.getParentDirectory());
     }
 
     setLayoutInternal (contents);
@@ -1156,61 +1156,62 @@ void LayoutSupport::setGridPositions (const juce::String& currentPath, const juc
         ids = expandTokens ({ idString });
 
     ids.trim ();
-    
+
     if (auto foundComp = compMap.findComponent (currentPath))
     {
         juce::ScopedValueSetter<juce::Component*> svs (curComponent, foundComp);
-        
+
         auto bounds = getBounds (0, component);
-        
+
         juce::Rectangle<int> rc (bounds.x, bounds.y, bounds.w, bounds.h);
-        
+
         using juce::operator""_px;
         using juce::operator""_fr;
-        
+
         juce::Grid grid;
-        
+
         auto cols = parse (getPropertyWithDefault (component, kCols, 1), 0);
         auto rows = parse (getPropertyWithDefault (component, kRows, 1), 0);
-        
+
         grid.columnGap  = juce::Grid::Px (parse (getPropertyWithDefault (component, kColGap, 0), 0));
         grid.rowGap     = juce::Grid::Px (parse (getPropertyWithDefault (component, kRowGap, 0), 0));
-        
+
         auto getTrackInfo = [] (const juce::String& str, int idx)
         {
             auto tokens = juce::StringArray::fromTokens (str, "/", "");
             tokens.trim();
             auto token = tokens[idx + 1];
-            
+
             if (token.endsWith ("px"))
                 return juce::Grid::TrackInfo (juce::Grid::Px (token.retainCharacters ("0123456789").getIntValue()));
-            
+
             if (token.endsWith ("fr"))
                 return juce::Grid::TrackInfo (juce::Grid::Fr (token.retainCharacters ("0123456789").getIntValue()));
-            
+
             return juce::Grid::TrackInfo (1_fr);
         };
-        
+
         for (auto i = 0; i < cols; i++)
             grid.templateColumns.add (getTrackInfo (ids[i], 0));
-        
+
         for (auto i = 0; i < rows; i++)
             grid.templateRows.add (getTrackInfo (ids[cols * i], 1));
-        
+
         for (auto id : ids)
         {
             const auto path = juce::String (currentPath) + "/" + id.upToFirstOccurrenceOf ("/", false, false).trim();
-            
+
             if (auto foundChildComp = compMap.findComponent (path))
                 grid.items.add (juce::GridItem (foundChildComp));
             else
                 grid.items.add (juce::GridItem());
         }
-        
+
         grid.performLayout (rc);
     }
 }
 
+/*
 void LayoutSupport::fileChanged (const juce::File& f, gin::FileSystemWatcher::FileSystemEvent)
 {
     if (f.existsAsFile() && layoutFiles.contains (f))
@@ -1228,6 +1229,7 @@ void LayoutSupport::fileChanged (const juce::File& f, gin::FileSystemWatcher::Fi
         setLayoutInternal (contents);
     }
 }
+*/
 
 int LayoutSupport::parse (const juce::var& equation, int equationIndex)
 {
diff --git a/modules/gin/modules/gin_gui/utilities/gin_layoutsupport.h b/modules/gin/modules/gin_gui/utilities/gin_layoutsupport.h
index 88ccc3b23c..89f06fb38d 100644
--- a/modules/gin/modules/gin_gui/utilities/gin_layoutsupport.h
+++ b/modules/gin/modules/gin_gui/utilities/gin_layoutsupport.h
@@ -264,7 +264,7 @@ private:
     }
     @endcode
 */
-class LayoutSupport : private gin::FileSystemWatcher::Listener
+class LayoutSupport //: private gin::FileSystemWatcher::Listener
 {
 public:
     /**
@@ -277,7 +277,7 @@ public:
     */
     LayoutSupport (juce::Component& parent_, std::function<std::pair<juce::Component*, bool> (const juce::String&)> factory_ = nullptr);
 
-    ~LayoutSupport() override;
+    ~LayoutSupport();// override;
 
     /**
         Generate JSON layout from existing component hierarchy.
@@ -367,10 +367,10 @@ protected:
     void setGridPositions (const juce::String& currentPath, const juce::var& component);
 
     inline static int refCount = 0;
-    inline static std::unique_ptr<FileSystemWatcher> watcher;
+    //inline static std::unique_ptr<FileSystemWatcher> watcher;
 
     // gin::FileSystemWatcher::Listener
-    void fileChanged (const juce::File&, gin::FileSystemWatcher::FileSystemEvent) override;
+    //void fileChanged (const juce::File&, gin::FileSystemWatcher::FileSystemEvent) override;
 
     ConstantsStack constants;
     std::map<juce::String, juce::var> macros;
diff --git a/modules/gin/modules/gin_plugin/components/gin_patchbrowser.cpp b/modules/gin/modules/gin_plugin/components/gin_patchbrowser.cpp
index b9db9235b7..9138665082 100644
--- a/modules/gin/modules/gin_plugin/components/gin_patchbrowser.cpp
+++ b/modules/gin/modules/gin_plugin/components/gin_patchbrowser.cpp
@@ -177,9 +177,13 @@ void PatchBrowser::deletePreset (int row)
     auto p = proc.getProgram (currentPresets[row]);
     if (p == nullptr)
         return;
-    
+
+   #if JUCE_ANDROID
+    // AAP: PluginAlertWindow crashes without an Activity inside AudioPluginViewService.
+    return;
+   #else
     auto ed = findParentComponentOfClass<ProcessorEditor>();
-    
+
     auto w = std::make_shared<gin::PluginAlertWindow> ("Delete preset '" + p->name + "'?", "", juce::AlertWindow::NoIcon, getParentComponent());
     w->addButton ("Yes", 1, juce::KeyPress (juce::KeyPress::returnKey));
     w->addButton ("No", 0, juce::KeyPress (juce::KeyPress::escapeKey));
@@ -191,6 +195,7 @@ void PatchBrowser::deletePreset (int row)
         if (r == 1)
             proc.deleteProgram (proc.getPrograms().indexOf (p));
     });
+   #endif
 }
 
 void PatchBrowser::editPreset (int row)
@@ -198,9 +203,12 @@ void PatchBrowser::editPreset (int row)
     auto p = proc.getProgram (currentPresets[row]);
     if (p == nullptr)
         return;
-    
+
+   #if JUCE_ANDROID
+    return;
+   #else
     auto ed = findParentComponentOfClass<ProcessorEditor>();
-    
+
     auto w = std::make_shared<gin::PluginAlertWindow> ("Edit preset:", "", juce::AlertWindow::NoIcon, getParentComponent());
     w->setLookAndFeel (&getLookAndFeel());
     w->addTextEditor ("name", p->name, "Name:");
@@ -244,6 +252,7 @@ void PatchBrowser::editPreset (int row)
             }
         }
     });
+   #endif
 }
 
 PatchBrowser::AuthorsModel::AuthorsModel (PatchBrowser& o)
@@ -387,8 +396,13 @@ void PatchBrowser::PresetsModel::listBoxItemClicked (int row, const juce::MouseE
    #else
     m.addItem ("Show file...", [f] { f.revealToUser(); });
    #endif
-    
-    m.showMenuAsync ({});
+
+    juce::PopupMenu::Options opts;
+   #if JUCE_ANDROID
+    if (auto* ed = owner.findParentComponentOfClass<ProcessorEditor>())
+        opts = opts.withParentComponent (ed);
+   #endif
+    m.showMenuAsync (opts);
 }
 
 
diff --git a/modules/gin/modules/gin_plugin/components/gin_plugineditor.cpp b/modules/gin/modules/gin_plugin/components/gin_plugineditor.cpp
index 1970966c82..a0901c870f 100644
--- a/modules/gin/modules/gin_plugin/components/gin_plugineditor.cpp
+++ b/modules/gin/modules/gin_plugin/components/gin_plugineditor.cpp
@@ -229,6 +229,10 @@ TitleBar::TitleBar (ProcessorEditor& e, Processor& p, PatchBrowser& pb)
         }
         
         auto opts = juce::PopupMenu::Options().withTargetComponent (programName).withDeletionCheck (programName);
+       #if JUCE_ANDROID
+        if (auto* ed = findParentComponentOfClass<ProcessorEditor>())
+            opts = opts.withParentComponent (ed);
+       #endif
         m.showMenuAsync (opts, [&] (int) { menuShowing = false; } );
     };
 
@@ -269,12 +273,18 @@ TitleBar::TitleBar (ProcessorEditor& e, Processor& p, PatchBrowser& pb)
     };
     addButton.onClick = [this]
     {
+       #if JUCE_ANDROID
+        // AAP: PluginAlertWindow uses juce::AlertWindow which adds itself to the
+        // desktop on construction; inside AudioPluginViewService there is no
+        // Activity, so that crashes. Skip the dialog on Android.
+        return;
+       #else
         Program* prog = nullptr;
-        
+
         int progIdx = slProc.getCurrentProgram();
         if (progIdx > 0)
             prog = slProc.getPrograms()[progIdx];
-        
+
         auto w = std::make_shared<gin::PluginAlertWindow> ("Create preset:", "", juce::AlertWindow::NoIcon, getParentComponent());
         w->setLookAndFeel (slProc.processorOptions.lookAndFeel.get());
         w->addTextEditor ("name", prog != nullptr ? prog->name : juce::String(), "Name:");
@@ -321,9 +331,13 @@ TitleBar::TitleBar (ProcessorEditor& e, Processor& p, PatchBrowser& pb)
                 }
             }
         });
+       #endif
     };
     deleteButton.onClick = [this]
     {
+       #if JUCE_ANDROID
+        return;
+       #else
         auto w = std::make_shared<gin::PluginAlertWindow> ("Delete preset '" + slProc.getProgramName (programs.getSelectedItemIndex()) + "'?", "", juce::AlertWindow::NoIcon, getParentComponent());
         w->addButton ("Yes", 1, juce::KeyPress (juce::KeyPress::returnKey));
         w->addButton ("No", 0, juce::KeyPress (juce::KeyPress::escapeKey));
@@ -338,10 +352,17 @@ TitleBar::TitleBar (ProcessorEditor& e, Processor& p, PatchBrowser& pb)
                 refreshPrograms();
             }
         });
+       #endif
     };
     infoButton.onClick = [this]
     {
+       #if JUCE_ANDROID
+        // AAP: About dialog path goes through juce::AlertWindow which crashes
+        // without an Activity.
+        return;
+       #else
         editor.showAboutInfo();
+       #endif
     };
     menuButton.onClick = [this]
     {
@@ -534,6 +555,9 @@ void TitleBar::showMenu()
     });
 
     m.addSeparator();
+   #if ! JUCE_ANDROID
+    // AAP: juce::FileChooser::launchAsync requires an Activity on Android,
+    // which isn't available inside AudioPluginViewService.
     if (slProc.midiLearn != nullptr)
     {
         m.addItem ("Load MIDI Map...", [this]
@@ -573,11 +597,17 @@ void TitleBar::showMenu()
                 });
         });
     }
+   #endif
 
     editor.addMenuItems (m);
 
     m.setLookAndFeel ( &getLookAndFeel());
-    m.showMenuAsync (juce::PopupMenu::Options().withTargetComponent (menuButton).withDeletionCheck (menuButton));
+    auto opts = juce::PopupMenu::Options().withTargetComponent (menuButton).withDeletionCheck (menuButton);
+   #if JUCE_ANDROID
+    if (auto* ed = findParentComponentOfClass<ProcessorEditor>())
+        opts = opts.withParentComponent (ed);
+   #endif
+    m.showMenuAsync (opts);
 }
 
 void TitleBar::comboBoxChanged (juce::ComboBox* c)
@@ -787,6 +817,11 @@ void ProcessorEditor::showAboutInfo()
     msg += "Copyright ";
     msg += juce::String (&__DATE__[7]);
 
+   #if JUCE_ANDROID
+    // AAP: juce::AlertWindow adds itself to the desktop on construction;
+    // inside AudioPluginViewService there is no Activity, so skip it.
+    juce::ignoreUnused (msg);
+   #else
     auto w = std::make_shared<gin::PluginAlertWindow> ("---- About ----", msg, juce::AlertWindow::NoIcon, this);
     w->addButton ("OK", 1, juce::KeyPress (juce::KeyPress::returnKey));
     w->setLookAndFeel (slProc.processorOptions.lookAndFeel.get());
@@ -795,6 +830,7 @@ void ProcessorEditor::showAboutInfo()
     {
         w->setVisible (false);
     });
+   #endif
 }
 
 void ProcessorEditor::refreshProgramsList()
diff --git a/plugin/Source/Panels.h b/plugin/Source/Panels.h
index 30c91ab..1f1458c 100644
--- a/plugin/Source/Panels.h
+++ b/plugin/Source/Panels.h
@@ -50,6 +50,11 @@ public:
         addControl (addButton);
         addButton->onClick = [this]
         {
+           #if JUCE_ANDROID
+            // AAP: juce::FileChooser::launchAsync requires an Activity, which
+            // isn't available inside AudioPluginViewService.
+            return;
+           #else
             auto chooser = std::make_shared<juce::FileChooser> (juce::String ("Load Wavetable"), juce::File(), juce::String ("*.wav"));
 
             auto chooserFlags = juce::FileBrowserComponent::canSelectFiles | juce::FileBrowserComponent::openMode;
@@ -61,6 +66,7 @@ public:
 
                 loadUserWavetable (f);
             } );
+           #endif
         };
 
         timer.startTimerHz (60);
@@ -90,6 +96,10 @@ public:
         auto sz = gin::getWavetableSize (f);
         if (sz <= 0)
         {
+           #if JUCE_ANDROID
+            // AAP: PluginAlertWindow crashes without an Activity in AudioPluginViewService.
+            proc.loadUserWavetable (idx, f, 2048);
+           #else
             auto w = std::make_shared<gin::PluginAlertWindow> ("Import Wavetable", "Wav file does not contain Wavetable metadata. What is the wavetable size?", juce::AlertWindow::NoIcon, getParentComponent());
             w->setLookAndFeel (proc.processorOptions.lookAndFeel.get());
             w->addComboBox ("size", { "256", "512", "1024", "2048" }, "Samples per table:");
@@ -106,6 +116,7 @@ public:
                 if (ret == 1)
                     proc.loadUserWavetable (idx, f, userSize);
             });
+           #endif
         }
         else
         {
@@ -180,7 +191,12 @@ public:
             for (auto itr : menus)
                 m.addSubMenu (itr.first, itr.second);
 
-            m.showMenuAsync ({});
+            juce::PopupMenu::Options opts;
+           #if JUCE_ANDROID
+            if (auto* ed = findParentComponentOfClass<gin::ProcessorEditor>())
+                opts = opts.withParentComponent (ed);
+           #endif
+            m.showMenuAsync (opts);
         }
     }
 
@@ -787,7 +803,12 @@ public:
                 proc.fxParams.distMode->setUserValue (3.0f);
             });
 
-            m.showMenuAsync ({});
+            juce::PopupMenu::Options opts;
+           #if JUCE_ANDROID
+            if (auto* ed = findParentComponentOfClass<gin::ProcessorEditor>())
+                opts = opts.withParentComponent (ed);
+           #endif
+            m.showMenuAsync (opts);
         };
 
         watchParam (proc.fxParams.distMode);
